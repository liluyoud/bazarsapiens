@page
@model BazarSapiens.Pages.Admin.Colaboradores.IndexModel

@{
    ViewData["Title"] = "Bazar - Colaboradores";
}

<h1>Colaboradores</h1>

<a asp-page="Create" class="btn btn-raised btn-info">Adicionar Colaborador</a>

<div class="card">
    <table class="table table-no-border">
        <thead>
            <tr>
                <th>Ordem</th>
                <th>Nome</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Colaboradores)
            {
            <tr>
                <td>@item.Ordem</td>
                <td>@item.Nome</td>
                <td width="30">
                    <a asp-page="./Edit" asp-route-id="@item.Id" title="Editar">
                        <i class="fa fa-pencil"></i>
                    </a>
                </td>
            </tr>
            }
        </tbody>
    </table>
</div>